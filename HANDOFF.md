# HANDOFF — carcheck: что делаем, как, что готово, что осталось

Документ для передачи работы в новую сессию. Прочитать целиком до того, как
что-то менять. Структура: **зачем → как решили → что сделано → что осталось**,
в конце — хронология, чтобы был виден ход мысли, а не только результат.

Репозиторий: **https://github.com/komikagze/carcheck** (публичный).
Дата обновления: 14 августа 2026.

---

# 1. ЧТО МЫ ХОТИМ СДЕЛАТЬ

Сайт, где по номеру израильского автомобиля можно проверить машину перед
покупкой. Открыт всем, без логина, бесплатно.

Два разных по природе блока информации:

### 1.1 Живой поиск (работает с самого начала)
Марка, год, цвет, пробег на последнем техосмотре, VIN, тип владения, отзывы
(ריקול), комплектация, цена по прайсу. Всё это **есть в data.gov.il прямо
сейчас**, запрашивается в момент поиска и никак не хранится у нас.

### 1.2 Накопленная история (главная ценность, ради неё всё затевалось)
Государство публикует только **текущий срез**: «пробег на последнем тесте —
129 982». Каким он был год назад — нигде не публикуется и нигде не хранится.

Поэтому мы **сами снимаем срез раз в неделю и копим разницу**. Это даёт то,
чего нет ни у одного сервиса:

- **история пробега по датам техосмотров** — видно динамику;
- **скрутка одометра** — пробег уменьшился между снимками, так не бывает;
- **смена номера двигателя** — двигатель меняли;
- **журнал изменений** — когда, какое поле, было → стало (цвет, VIN, топливо,
  тип владения, модель двигателя);
- **разбор истории владения** — сколько рук, сколько времени у каждой,
  сколько раз машина прошла через перекупщика.

### 1.3 Ограничение, встроенное в замысел
Данные по **одной** машине должны быть доступны любому желающему без логина.
Но **выгрузить всю базу целиком** должно быть непрактично. Это не паранойя, а
осознанное условие задачи с самого начала.

---

# 2. КАК МЫ РЕШИЛИ ЭТО ДЕЛАТЬ

## 2.1 Общая схема

```
                    data.gov.il (открытый JSON API)
                              |
        ┌─────────────────────┴──────────────────────┐
        |                                            |
   ЖИВОЙ ПОИСК                              СБОРЩИК раз в неделю
   (браузер спрашивает                      (GitHub Actions)
    напрямую, ничего                                |
    не храним)                                      v
                                        collector/collector.py
                                                    |
                                          локальная SQLite (~1.6 ГБ)
                                                    |
                        ┌───────────────────────────┼──────────────────────┐
                        v                           v                      v
              GitHub Release            export/turso_upload.py     export/export.py
              (тег db-latest)                      |                       |
         база между прогонами                      v                       v
                                                 TURSO              статика в gh-pages
                                          (облачный SQLite,                |
                                           хранит историю)                 v
                                                  ^               CLOUDFLARE WORKERS
                                                  |                (хостинг сайта)
                                                  └────────────────────────┘
                                                  worker/index.js
                                            читает Turso по ОДНОМУ номеру
```

## 2.2 Что делает Cloudflare, а что Turso — ЭТО ГЛАВНАЯ ПУТАНИЦА

Их роли часто путают, потому что Cloudflare мы **и используем, и частично
отказались**. Разложим:

| | Роль | Статус |
|---|---|---|
| **Cloudflare Workers** | Хостинг сайта: раздаёт статику из `public/` и выполняет серверную функцию `worker/index.js` | **ИСПОЛЬЗУЕТСЯ.** Это наш хостинг |
| **Cloudflare Pages** | Был хостингом по замыслу | **НЕДОСТУПЕН.** Cloudflare убрал создание новых проектов |
| **Cloudflare KV** | Задумывался как хранилище накопленной истории | **ОТКАЗАЛИСЬ.** Заменён на Turso |
| **Turso** | Хранилище накопленной истории (облачный SQLite) | **ИСПОЛЬЗУЕТСЯ** вместо KV |

Проще говоря: **Cloudflare — это хостинг, Turso — это база данных.** Раньше
предполагалось, что и то и другое даст Cloudflare, но его база (KV) не подошла
по лимитам, поэтому базу взяли у другого провайдера, а хостинг остался за
Cloudflare — только не Pages, а Workers.

### Почему Worker, а не Pages Function (август 2026)
Pages-проект создать невозможно: в панели остался только диалог «Create a
Worker». Поэтому единственный файл `functions/api/history/[plate].js` переписан
в обычный воркер `worker/index.js`.

Cloudflare предлагает компилировать папку `functions/` командой
`wrangler pages functions build`. **Мы этого не делаем:** маршрут ровно один,
точка входа была ровно одна, обвязка заняла полтора десятка строк. Это убирает
из конвейера шаг сборки, npm-зависимость и команду, которую сам Cloudflare
называет переходной. Логика Turso, рейт-лимит и формат ответа не менялись.

Ключевое следствие для раскладки: у Workers **ассеты отдаются до кода**, и
корнем сайта становится ровно одна папка. Поэтому статика лежит отдельно, в
`public/` — иначе `wrangler.jsonc` и `worker/` уехали бы в публичные файлы.
Адреса при этом не изменились: `/meta.json` и `/static/app.js` на прежних местах.

### Почему отказались от Cloudflare KV
На первом реальном прогоне заливка упала с ошибкой 429. Выяснилось: бесплатный
тариф KV разрешает **1000 операций записи в СУТКИ**, а машин у нас **2.4 млн**.
Разница в тысячи раз — залить базу было невозможно в принципе, не «медленно», а
именно никогда.

### Почему именно Turso
Перебрали варианты (все бесплатные тарифы):

| Вариант | Место | Лимит записи | Вердикт |
|---|---|---|---|
| Cloudflare KV | — | 1000/**сутки** | Не подходит вообще |
| Supabase | 0.5 ГБ | — | Мало места (у нас ~350 МБ и растёт ~130 МБ/год) |
| Neon | 0.5 ГБ | — | То же самое |
| Cloudflare D1 | 5 ГБ | 100 тыс./**сутки** | Первая заливка растянулась бы на ~25 дней |
| **Turso** | **5 ГБ** | **10 млн/МЕСЯЦ** | **Взяли.** 4.9 млн строк проходят за один прогон |

Бонус: Turso — это буквально SQLite, тот же диалект, что в локальной базе
сборщика. Схема и запросы переносятся почти один в один.

Важное техническое требование, которое отсекло часть кандидатов: серверная
функция Cloudflare работает на edge-рантайме и **не умеет открывать обычные
TCP-соединения к Postgres/MySQL**. Хранилищу обязательно нужен HTTP API.
У Turso он есть (`/v2/pipeline`).

## 2.3 Почему база не в git, а в GitHub Release
Локальная SQLite весит ~1.6 ГБ. GitHub блокирует push файлов больше 100 МБ,
а еженедельный коммит такого файла раздувал бы историю навсегда. GitHub Release
— лимит 2 ГБ на файл, вне истории git, без квоты трафика (в отличие от Git LFS).
Между прогонами база живёт там под тегом `db-latest`.

## 2.4 Почему JSON API, а не скачивание CSV
Изначально сборщик качал сырые CSV. Оказалось, CDN `e.data.gov.il` стоит за
защитой от ботов (JS-челлендж): обычный HTTP-клиент получает HTML-заглушку
вместо файла. Проверено вживую — в браузере тот же URL отдаёт нормальный CSV.

Решение: тянуть постранично через `datastore_search` — **тот же самый
официальный публичный API**, которым уже пользуется живой поиск. Это не обход
защиты, а другой штатный интерфейс того же портала.

## 2.5 Как устроена защита от массовой выгрузки
- Данные лежат в Turso, к которой у браузера посетителя **нет прямого доступа**.
- Наружу торчит только функция `functions/api/history/[plate].js`, принимающая
  **ровно один номер за раз**. Эндпоинта «дай список» или «дай дамп» не
  существует в принципе.
- Плюс рейт-лимит 25 запросов/мин на IP (таблица `rate_limit` в той же базе).

Честная оценка: это **затрудняет и делает нерентабельным** перебор, но не
гарантирует невозможность (можно менять IP). Так и записано в README.

## 2.6 Интерфейс на иврите
Аудитория ивритоязычная, и данные из data.gov.il **уже приходят на иврите**.
Поэтому весь UI на иврите, `<html lang="he" dir="rtl">`, CSS выправлен под RTL.
Словарь `VALUE_TRANSLATIONS` (иврит→русский) удалён намеренно — переводить
значения стало незачем, а это был источник ошибок.

**Комментарии в коде, README, FILES.md и этот файл остаются на русском** — это
заметки разработчику. Проверено автоматически: кириллицы в самом интерфейсе нет.

---

# 3. ЧТО УЖЕ СДЕЛАНО

## 3.1 Код и функциональность

- ✅ Интерфейс полностью переведён на иврит, RTL проверен в браузере
- ✅ Сборщик переписан на постраничный JSON API
- ✅ Собираются 3 ресурса: история (2.4 млн), основной реестр (4.2 млн),
  владение (5.3 млн)
- ✅ История пробега **с реальными датами техосмотров**
- ✅ Журнал изменений по 14 полям
- ✅ Детект скрутки одометра и смены двигателя
- ✅ Разбор истории владения: руки, сроки, перекупщики, флаг перепродаж
- ✅ Хранилище переведено с Cloudflare KV на Turso
- ✅ Заливка сделана возобновляемой, с ретраями
- ✅ Всё запушено в GitHub (8 коммитов)

## 3.2 Инфраструктура (настроена пользователем)

- ✅ Turso: база создана, `TURSO_DATABASE_URL` и `TURSO_AUTH_TOKEN` заведены
  в GitHub Secrets
- ✅ GitHub Actions: включены права «Read and write permissions»
- ❗ **Pages-проекта нет и не будет — Cloudflare убрал их создание.**
  Проверено 14.08: в дашборде диалог называется «Create a Worker», вкладки Pages
  в нём нет вообще. Документация подтверждает — актуальный сценарий
  «Create application → Import a repository», и это Workers; официальный гайд
  теперь называется «Migrate from Pages to Workers».

  Поэтому **сайт переехал на обычный Worker со static assets** (см. §2.2).
  Ранее в этом файле были подробные шаги «создать Pages-проект» — они
  нерабочие, выполнить их в панели невозможно.

- ✅ Воркер `carcheck` создан, `workers.dev` включён:
  **https://carcheck.anton-golodny.workers.dev/**
- ❌ **В воркере лежит заглушка Cloudflare**, а не наш код. Проверено через
  коннектор (`workers_get_worker_code`):
  ```js
  export default { async fetch(request, env) { return new Response("Hello world") } }
  ```
  Сборка из репозитория падала («Latest build failed»), потому что репозиторий
  был в формате Pages (папка `functions/`), а Worker такую раскладку не понимает.
  Код под Workers подготовлен (§2.2), осталось настроить сборку — см. §4.2.

  **Частая путаница:** «мы же отказались от Cloudflare» — отказались только от
  его базы **KV**. Cloudflare остаётся хостингом, просто теперь в виде Workers,
  а не Pages. Turso хранит данные, но показать сайт людям не может.
- ⚠️ Старые секреты `CF_API_TOKEN`, `CF_ACCOUNT_ID`, `CF_KV_NAMESPACE_ID` и
  KV namespace `carcheck-history` **больше не используются** — можно удалить

## 3.3 Что реально лежит в локальной базе (~1.6 ГБ)

| Таблица | Строк | Что |
|---|---|---|
| `current_state` | 2 439 972 | Состояние по ресурсу истории (пробег, двигатель) |
| `current_state_main` | 4 164 345 | Состояние по основному реестру (цвет, VIN, владение, топливо, модель) |
| `odometer_readings` | 2 424 894 | Пробег с реальной датой техосмотра |
| `field_changes` | 2 447 515 | Журнал «когда / что / было → стало» |
| `ownership` | 5 191 074 | История владения |
| `engine_changes` | 47 | Смены номера двигателя |
| `odometer_anomalies` | 71 | Падения пробега (скрутка) |

## 3.4 Первые живые срабатывания (ночной снимок 13→14 августа)

Это доказательство, что механизм реально работает, а не теоретически:

- новых машин: **938**
- изменений полей: **7 472** (у 7 428 машин)
- **смен двигателя: 47** — например `66814601`: `DAD C24532 → C44351`
- **аномалий пробега: 71** — например `24497602`: **178 527 → 91 345 км**
  (минус 87 тысяч, классическая скрутка)
- разбивка изменений километража: 5 304 обычный рост, 1 052 появился впервые,
  71 падение

---

# 4. ЧТО ОСТАЛОСЬ СДЕЛАТЬ

## 4.1 ЗАЛИВКА В TURSO ОСТАНОВЛЕНА ДО 1 СЕНТЯБРЯ 2026 — квота выбрана

**Прогон #3 отменён вручную 14.08 в 08:40 UTC**, не доработав. Причина не
техническая: Turso показал **Quota Critical, 98% — 9 758 196 записей из 10 млн
в месяц**. На бесплатном тарифе превышение любого лимита блокирует **весь
аккаунт**, поэтому прогон остановили, не дожидаясь конца.

⚠️ **Остановить успели не до конца: итог 10 194 221, то есть за чертой.**
Счётчик доехал уже после отмены, и это важно понимать правильно. Последняя
строка лога прогона #3 — 08:34:07, а завершился он 08:40:07: шесть минут без
единой записи, хотя пишет он каждые 10 000 строк. Это **непрослитый буфер
вывода** — Python в CI буферизует stdout, и строки, записанные перед
принудительным завершением, до файла не доехали. При скорости ~36 тыс. строк
в минуту за эти шесть минут ушло ещё ~218 тыс. строк ≈ 436 тыс. единиц квоты.
Наблюдаемый скачок — 436 025, сходится.

Практический вывод на будущее: **отмена прогона не мгновенна**, и по логу
нельзя судить, где он реально остановился. Запускать питон с `-u`, иначе
последние строки теряются и картина искажается ровно в тот момент, когда она
нужнее всего.

### Главный урок: квота считается НЕ в строках

Turso тарифицирует запись **каждой индексной записи наравне со строкой таблицы**.
Обе наши таблицы имеют по одному индексу, значит **~2 единицы квоты на строку**:

| Таблица | Из чего 2 | |
|---|---|---|
| `odometer` | строка + UNIQUE-индекс от `PRIMARY KEY (plate, test_date)` | |
| `changes` | строка + `idx_changes_plate` | |

Проверено на фактах, а не выведено: по логам залито 4 994 894 строки
(940 000 в прогоне #2 + 2 424 894 `odometer` и 1 630 000 `changes` в прогоне #3),
Turso насчитал 9 758 196 записей. **Отношение 1.95.**

Раньше в этом файле стояло «по квоте ~5.8 млн из 10 млн» — **это была ошибка**,
считались строки. Реально одна полная заливка стоит ~9.8 млн, то есть **всю
месячную квоту разом**. Схема «раз в неделю перезаливаем всё» на бесплатном
тарифе не работает в принципе.

Это тот же класс ошибки, что был с Cloudflare KV: лимит взят из документации
и посчитан не в тех единицах. Урок — проверять квоту на живом прогоне по
счётчику провайдера, а не по своей арифметике.

### Что уже лежит в Turso

- `odometer` — **залит полностью** (2 424 894). Это главная ценность проекта,
  история пробега по датам техосмотров. То есть сайт уже может работать.
- `changes` — 1 630 000 из 2 447 515, курсор сохранён, дозальётся с места.
- Storage 312.51 МБ — места полно, оно тут вообще ни при чём.

### Как решили действовать (14.08)

Сбор данных продолжается, заливка в Turso ждёт обнуления счётчика **1 сентября
2026**. В `weekly.yml` для этого стоит выключатель `TURSO_UPLOAD_ENABLED: "false"`
на шаге заливки.

**Выключен именно шаг, а не workflow целиком, и это принципиально:** источник
публикует только ТЕКУЩИЙ срез, поэтому пропущенный недельный снимок теряется
навсегда. Сбор и заливка базы в Release идут как обычно, история копится;
в Turso она уедет после обнуления квоты разом за всё пропущенное время.

### Заливка УЖЕ инкрементальная — не переделывать

Была записана задача «сделать заливку инкрементальной». **Это ошибка, задачи
нет.** Закладка (`odometer_cursor`, `changes_cursor`) лежит в таблице
`sync_state` внутри самой Turso и переживает не только сбой, но и промежуток
между прогонами — см. докстринг `sync_odometer`: «одного курсора хватает и для
докачки после сбоя, и для инкремента на следующей неделе». Каждую неделю
выбираются только строки больше закладки.

Прогон #3 лил всё с нуля не поэтому: это была **первая полная загрузка**, а
прогон #2 работал ещё до `61a0eb5` и закладку не сохранял. Сейчас сохраняет.
Недельная дельта (~8.4 тыс. строк ≈ 17 тыс. единиц квоты, 0.17%) получится сама.

Дорого стоит только первая загрузка, и она бывает один раз.

### Главное про остаток: залито 1.63 млн ПУСТЫШЕК, журнал не залит вообще

| | Строк | Единиц квоты |
|---|---|---|
| Уже залито | 1 630 000 | — |
| Осталось как есть | 817 515 | 1 635 030 |
| **Из них содержательных** | **7 543** | **15 086** |

Разбор `changes` по типу:

| Тип | Строк |
|---|---|
| `new_car` (поле пустое) | 2 439 972 |
| `changed` | 7 472 |
| `anomaly` | 71 |

**99.7% таблицы — записи «машина увидена впервые»** с пустыми `field`,
`old_value`, `new_value`. Про историю конкретной машины они не сообщают ничего.

И все 7 543 содержательные записи лежат **в конце по номеру**, поэтому
**не залиты ни одной**. Причина: `new_car` создавались первыми, когда база
впервые увидела 2.44 млн машин, а реальные изменения нашлись только при
сравнении срезов 13→14 августа и получили самые большие `id`. Заливка идёт по
возрастанию `id`. Итог: **все 1.63 млн отправленных строк — пустышки, а весь
ценный журнал до Turso не доехал.**

### План на 1 сентября

1. **В заливку добавить отбор `field IS NOT NULL`** — пустышки перестанут
   уезжать. Тогда остаток журнала стоит 15 тыс. единиц вместо 1.64 млн.
2. **В воркере при чтении отфильтровать `change_kind != 'new_car'`**, чтобы уже
   залитые 1.63 млн пустышек не лезли в интерфейс. Удалять их из Turso не надо:
   удаление тоже тратит квоту, а места они занимают немного (всего 312 МБ).
   ⚠️ Интерфейс их сейчас ПОКАЗЫВАЕТ строкой «הרכב נוסף למאגר» с прочерками
   (`app.js`, `CHANGE_KIND_LABELS`). После фильтра машины, у которых кроме этой
   записи ничего нет, станут отдавать «история не накоплена» — это честнее.
3. Перед включением прогнать отбор **вхолостую** и убедиться, что собирается
   7 543 строки, а не 817 515.
4. Вернуть `TURSO_UPLOAD_ENABLED: "true"` в `weekly.yml`.

Решение от 14.08: **ждём обнуления квоты 1 сентября**, хотя остатка (241 804
единицы) хватило бы долить журнал сразу — запас был бы 16-кратный. Выбрана
осторожность: превышение лимита блокирует весь аккаунт. Цена решения — до
сентября сайт показывает пробег без журнала изменений.

Признак правильного кода в логе: строки `odometer: залито N строк...`
(в старом было `Залито N/4872409`).

Полезное: логи Actions теперь читаются программно, см. §5.
```bash
export PATH="$PATH:/c/Program Files/GitHub CLI"
gh api "repos/komikagze/carcheck/actions/runs?per_page=3" \
  --jq '.workflow_runs[] | {id, status, conclusion, run_started_at}'
```

## 4.2 ✅ СДЕЛАНО 14.08.2026 — САЙТ РАБОТАЕТ

**https://carcheck.anton-golodny.workers.dev/** — живой, отдаёт наш код.

Проверено сквозняком: статика (`/`, `/static/app.js`, `/static/style.css`,
`/meta.json`) — 200; задеплоенный скрипт сверен через API Cloudflare, это
именно `worker/index.js`; `/api/history/24497602` — 200 с реальными данными
из Turso; в браузере отчёт отрисовался целиком.

Что для этого понадобилось, все три пункта — руками в панели:

1. **Settings → Build**: ветка **`gh-pages`**, deploy command `npx wrangler deploy`,
   build command пустой, root `/`.
2. **Settings → Variables and Secrets**: `TURSO_DATABASE_URL` и `TURSO_AUTH_TOKEN`.
3. **Settings → Domains & Routes**: включён `workers.dev` — это и был ответ на
   «ссылку не даёт» («No URLs enabled» именно про него).

### Три ловушки, на которых это встало (не наступать снова)

**а) `gh-pages` надо пересобрать ПОСЛЕ правок кода.** Настройки сборки были
верные, но ветка содержала старую раскладку (формат Pages, без `wrangler.jsonc`),
потому что генерируется прогоном, а он с момента правок не запускался.
`wrangler deploy` без конфига падает. Лечится одним `workflow_dispatch`.

**б) Secret, а не Variable.** `npx wrangler deploy` перезаписывает конфигурацию
воркера тем, что описано в `wrangler.jsonc`. Блока `vars` там нет намеренно —
секреты не должны лежать в репозитории. Поэтому переменные, заведённые типом
**Text**, деплой сносит, и воркер отдаёт 500 «אחסון ההיסטוריה אינו מוגדר».
Заводить только типом **Secret** — он зашифрован и деплой переживает.

**в) Токен Turso нужен ПИШУЩИЙ.** С read-only токеном сайт падает с 502:
```
Turso: Operation was blocked: SQL write operations are forbidden
```
Причина не в чтении истории, а в рейт-лимите: воркер на каждом запросе пишет
счётчик в таблицу `rate_limit`.

### ✅ Рейт-лимит переделан (14.08) — сбой записи больше не роняет запрос

Была вывернутая логика: **механизм защиты от скрапинга сам расходовал квоту.**
Каждый просмотр страницы писал счётчик в Turso (~4 единицы), перебор номеров
упирался в лимит, но каждая отбитая попытка всё равно списывала записи, а
исчерпание квоты блокирует аккаунт целиком — то есть перебор клал сайт, даже
будучи успешно отражённым. И любой сбой этой записи ронял ВЕСЬ запрос с 502,
хотя данные читаются прекрасно (ровно так сайт упал на read-only токене).

Теперь ограничение необязательное: если запись не прошла, запрос повторяется
в режиме «только чтение» и история всё равно отдаётся. Вторая поездка до базы —
только в этом случае, обычный путь по-прежнему один HTTP-вызов.

Проверено в браузерном движке с подменой `fetch`, пять сценариев:

| Сценарий | Вызовов | Результат |
|---|---|---|
| Норма | 1 (4 запроса) | 200, данные на месте |
| Запись запрещена | 2 (4, затем 2) | **200, история отдана** |
| `RATE_LIMIT_DISABLED=1` | 1 (2 чтения) | 200, записи нет вовсе |
| Лимит превышен | 1 | 429 |
| База недоступна | 2 | 502 с внятным текстом |

**Сейчас стоит `RATE_LIMIT_DISABLED: "1"`** (в `wrangler.jsonc`, блок `vars`) —
квота превышена, и пока мы за чертой, любая лишняя запись приближает блокировку.
**После 01.09.2026 вернуть `"0"`.**

Флаг объявлен в конфиге, а не в панели, намеренно: `wrangler deploy`
перезаписывает обычные переменные воркера тем, что указано в `wrangler.jsonc`,
поэтому выставленное руками значение он бы стёр при первом деплое. Секретов это
не касается, они хранятся отдельно (см. ловушку «б» выше).

### Что сайт показывает сейчас, а что нет

По `24497602` отдаётся пробег с реальной датой теста (2026-08-12, 91 345 км),
а вот **предупреждения о скрутке нет** — хотя локально оно есть. Это не баг:
записи об аномалиях не залиты в Turso, см. §4.1. Живое подтверждение разбора
оттуда же: в ответе `anomalies: []`, а в журнале — единственная строка-пустышка
`new_car`. Появится после заливки 1 сентября.

⚠️ **`https://carcheck.pages.dev` — НЕ наш сайт.** Проверено 14.08.2026: адрес
занят посторонним проектом, там англоязычный лендинг iOS-приложения
«Car Photo Checklist» (`carphotochecklist.com`), отдаёт 200. Ловушка: сайт
отвечает, только чужой. С переездом на Workers это уже неважно — поддомен
`workers.dev` выдаётся **внутри аккаунта**, а не глобально, поэтому имя
`carcheck` конфликтов не создаёт.

После деплоя — проверить тестовые номера из §7 и убедиться, что блок
«היסטוריה שנצברה» показывает данные из Turso, а не «история не накоплена»:
```bash
curl -s "https://carcheck.anton-golodny.workers.dev/api/history/24497602" | head -c 400
```

Не путать с URL **базы**: `libsql://carcheck-komikagze.aws-ap-northeast-1.turso.io`
— это Turso, туда ходит воркер, а не браузер.

## 4.3 Заполнится само со временем (не баг, так задумано)

- **Километраж по владельцам** — колонка есть, но пустая у всех. Нужны минимум
  два показания одометра, охватывающих период владения; сейчас у каждой машины
  ровно одно (собрано 14.08). Начнёт заполняться, когда машины пройдут следующие
  техосмотры. Для владельцев **до августа 2026 не появится никогда** — мы просто
  не видели их показаний.
- **Изменения полей основного реестра** — базовое состояние записано 14.08,
  сравнивать было не с чем. Срабатывания пойдут со следующего прогона.
- **История нескольких тестов на машину** — та же логика, копится по неделям.

## 4.4 Не сделано намеренно

- `server/price_meha.py` — интеграция с מחירון העם (carlistprice.mot.gov.il).
  Сайт целиком на JS, реальные адреса API вытащить не удалось. Нужен
  `Copy as cURL` из DevTools от пользователя; каркас готов и ждёт.
- Строгая защита от скрапинга (Turnstile/капча). Осознанно отложено, текущий
  рейт-лимит признан достаточным компромиссом.

---

# 5. ОКРУЖЕНИЕ (иначе будет больно)

| Что | Где |
|---|---|
| Рабочая копия | `C:\Users\komik\carcheck` |
| Python venv | `C:\cc_venv` → `/c/cc_venv/Scripts/python.exe` |
| Локальная база | `collector/db/carcheck.sqlite3` (~1.6 ГБ, **в git не идёт**) |
| Python | 3.13 |

**Ловушки:**

- venv лежит вне проекта (`C:\cc_venv`), потому что исходная папка сессии была
  слишком глубоко вложена и упиралась в лимит длины пути Windows — `venv` там падал.
- **`gh` CLI ЕСТЬ** — `C:\Program Files\GitHub CLI\gh.exe`, но **не в PATH**,
  поэтому `which gh` врёт «command not found». Запускать:
  ```bash
  export PATH="$PATH:/c/Program Files/GitHub CLI" && gh run list
  ```
  Авторизация активна (`komikagze`, права `repo` и `workflow`).
  - Git Bash переписывает аргумент вида `/repos/...` в путь файловой системы.
    Ведущий слэш **опускать**: `gh api "repos/komikagze/carcheck/..."`.
  - Лог **идущего** джоба скачать нельзя (404 `BlobNotFound`) — GitHub выкладывает
    его только после завершения. Живой прогон смотреть через статусы шагов.
- Питон запускать с `PYTHONIOENCODING=utf-8`, иначе `UnicodeEncodeError` на
  иврите и кириллице.
- Существует **вторая копия проекта** в служебной папке сессии. Все правки — по
  абсолютному пути `C:\Users\komik\carcheck`, иначе легко править одну копию,
  а проверять другую (я на этом уже обжёгся).

---

# 6. ГРАБЛИ, УЖЕ СОБРАННЫЕ (не наступать снова)

### 6.1 JOIN: обязателен `CAST(... AS TEXT)`
```sql
ON c.plate = CAST(CAST(TRIM(s.mispar_rechev) AS INTEGER) AS TEXT)
```
- **Внутренний `AS INTEGER`** срезает ведущие нули: из API приходит `07488339`,
  в базе лежит `7488339`. Без него такие машины КАЖДУЮ неделю считались бы новыми.
- **Внешний `AS TEXT`** нужен для скорости: без него сравнение TEXT-колонки с
  INTEGER-выражением не даёт использовать индекс, план вырождается в `SCAN`
  (2.4 млн × 2.4 млн) — прогон висел **16+ минут**. С ним — **1.5 секунды**.

Планы проверять через `EXPLAIN QUERY PLAN`, а не на глаз.

### 6.2 Массовые вставки — только внутри явной транзакции
`conn` открыт с `isolation_level=None`. Без явного `BEGIN` sqlite3 коммитит
каждую строку — миллионы fsync, часы простоя. Оборачивать в `db.transaction(conn)`.

### 6.3 staging-таблицы удалять + VACUUM
Иначе остаются навсегда и удваивают размер базы (было 1.19 ГБ вместо 600 МБ).
Удаляются в конце прогона (`drop_staging_tables`), **не** сразу после каждого
ресурса — `build_odometer_readings` джойнит `staging_history` со `staging_main`.

### 6.4 Дублирующие индексы
У `ownership` есть `PRIMARY KEY (plate, baalut_dt, baalut)` — отдельный индекс
по `plate` был бы дублем (leftmost prefix). Убран.

### 6.5 Основной реестр — только нужные поля
Все 24 колонки = ~2 ГБ RAM, рискованно для раннера. Запрашиваем 9 через
`fields=` → 418 МБ. См. `COLLECTED_RESOURCE_FIELDS` в `shared/refdata.py`.

### 6.6 Заливка в Turso: ретраи + возобновляемость
Упала на 940 тыс. из 4.87 млн с 502. Было две ошибки:
- ретраев не было (сотни запросов подряд — 502 практически неизбежна);
- курсор двигался только после ПОЛНОГО успеха → упавший прогон терял весь
  прогресс, следующий начинал с нуля и падал там же. Бесконечный цикл,
  сжигающий ~1 млн записей квоты за попытку.

Сейчас: до 5 попыток с растущей паузой, курсор пишется **после каждой пачки**.

### 6.7 Re-run в GitHub Actions берёт СТАРЫЙ коммит
«Re-run jobs» и «Re-run failed jobs» перезапускают тот же commit SHA. После
исправления кода нужен **свежий** запуск через «Run workflow».

### 6.8 Тип КПП по машине отследить нельзя
`automatic_ind` есть **только в каталоге моделей**, не в реестре по машине.
Это свойство модели, а не экземпляра — «поменяться» физически не может.
Ближайший честный сигнал — смена `degem_cd` (переклассификация), она отслеживается.

### 6.9 Перекупщик (סוחר) — не владелец
Транзитная точка, машина у него не эксплуатируется. Не получает номер руки,
километраж ему не считается. В таблице — обычная строка с пустой ячейкой руки.

---

# 7. КАК ПРОВЕРЯТЬ ЛОКАЛЬНО

```bash
cd /c/Users/komik/carcheck

# сборщик (обычный прогон)
/c/cc_venv/Scripts/python.exe collector/collector.py

# принудительный пересбор, даже если источник не менялся
# (нужно после изменения НАШЕЙ схемы, чтобы наполнить новые таблицы)
CARCHECK_FORCE=1 /c/cc_venv/Scripts/python.exe collector/collector.py

# статика
/c/cc_venv/Scripts/python.exe export/export.py

# локальный сервер (токен печатается в консоль)
/c/cc_venv/Scripts/python.exe server/app.py
```

**Тестовые номера с известным поведением:**

| Номер | Чем интересен |
|---|---|
| `5156286` | Обычная: 1 владелец, 9 лет, без предупреждений |
| `24497602` | **Скрутка одометра**: 178 527 → 91 345 км |
| `66814601` | **Смена двигателя**: DAD C24532 → C44351 |
| `15330101` | 22 записи владения → 8 рук, 16 перекупов, оба флага |
| `1003087` | Лизинг → перекуп → частное (чистый пример цепочки) |
| `2823923`, `2947251` | Старые (до 2017), истории владения нет |

---

# 8. ХРОНОЛОГИЯ: как мы сюда пришли

Полезно, чтобы понимать, почему код выглядит именно так.

**Исходная точка.** Проект был написан «вслепую» — файл за файлом, без
единого запуска кода. Интерфейс был на русском. Задача звучала так: перевести
интерфейс на иврит, впервые реально запустить, проверить и запушить на GitHub.

**1. Перевод на иврит.** UI переведён, `dir="rtl"`, CSS выправлен, словарь
иврит→русский удалён за ненадобностью.

**2. Первый запуск сборщика — падение.** Скачивание CSV отдавало HTML вместо
данных. Разобрались: CDN за анти-бот защитой. Переписали на постраничный JSON API.

**3. Второй запуск — зависание.** Висел 16+ минут без движения. Причина: вставки
без явной транзакции (миллионы отдельных коммитов). Обернули в транзакцию.

**4. База выросла до 1.19 ГБ.** Нашли две причины: staging-таблицы не удалялись
и дублирующий индекс. Почистили → 607 МБ.

**5. Не удалось запушить.** GitHub блокирует файлы >100 МБ. Решение: база уезжает
в GitHub Release, а не в git. Репозиторий стал 239 КБ.

**6. Первый прогон GitHub Actions — упал на заливке в Cloudflare KV** с 401.
Меняли токен, пересоздавали — не помогало.

**7. Вскрылась настоящая причина.** Письмо от Cloudflare: бесплатный KV — **1000
записей в сутки**, у нас 2.4 млн машин. Тупик по архитектуре, а не по токену.

**8. Выбор нового хранилища.** Пользователь: «всё должно быть бесплатно».
Перебрали Supabase, Neon, Cloudflare D1, R2, Backblaze, Google Drive. Проверили
актуальные лимиты по документации. Выбрали **Turso** (5 ГБ, 10 млн записей/мес,
и это SQLite). **Cloudflare Pages при этом остался хостингом.**

**9. Расширение данных по просьбе пользователя:**
- журнал изменений «когда / что / было → стало» с читаемыми названиями;
- пробеги **с датами техосмотров** — для этого пришлось дополнительно собирать
  основной реестр, потому что в ресурсе с километражом даты теста нет вообще;
- отслеживание полей реестра: цвет, VIN, тип владения, топливо, модель двигателя;
- разбор владения: перекуп не считается рукой, сроки владения, флаг перепродаж.

**10. По пути найдены и исправлены свои же баги:** сломанный JOIN по ведущим
нулям, потеря индекса из-за типов, 2 ГБ памяти на лишних колонках.

**11. Заливка в Turso упала на 940 тыс. строк** с 502. Исправлено: ретраи +
возобновляемый курсор. Запущен прогон #3, он идёт.

---

# 9. СТИЛЬ РАБОТЫ (важно)

- Проект писался без выполнения кода, поэтому **любое утверждение проверять
  запуском**, а не рассуждением. Практически все серьёзные проблемы выше
  вылезли только на живых данных и ни одна — при чтении кода.
- Обе версии интерфейса (`server/static/app.js` и
  `export/dist_template/static/app.js`) — почти дубликаты и держатся
  синхронными намеренно. Меняя логику в одной, переносить во вторую.
- **Не выдумывать данные, которых нет.** Если поля нет в источнике (КПП по
  машине, дата владения точнее месяца, пробег до августа 2026) — честно сказать
  об этом, а не подгонять и не прятать за прочерком без объяснения.
