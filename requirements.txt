# Питон-зависимости. Намеренно минимальный список — collector/ и export/ используют
# только стандартную библиотеку (urllib, csv, sqlite3, gzip, hashlib), чтобы сборщик
# одинаково легко ставился и на Windows у пользователя, и на GitHub-хостед раннере.
Flask==3.0.3
